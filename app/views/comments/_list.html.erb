<% post.comments.each do |comment| %>
  <div class="comment">
    <span class="comment-author"><%= comment.user.username %></span>
    <%= comment.body %>
    <div>
      <%= button_to "+/- 1", comment_likes_path(comment) %>
    </div>
  </div>
<% end %>
